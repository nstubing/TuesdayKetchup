@model TuesdayKetchup.Models.HomeInfo

@{
    ViewBag.Title = "EditBanner";
}

<h2>Edit Banners</h2>

    <div class="row">
        <div class="col-md-4" style="text-align:center;">
            @using (Html.BeginForm("EditBanner1", "Admin", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.Id)
            <p>Current image</p>
            <img src=@ViewBag.Picture1 style="width:90%; border-color:#1a1a1a; border-style:solid; border-width:.5em;" />
            <div class="editor-field" style="text-align:center; padding-top:1em;">
                <input id="Image" title="Upload new image"
                       type="file" name="file1" />
            </div>
            <div style="text-align:center; padding-top:1em;">
                    <input type="submit" value="Save Changes" class="btn btn-default" />
            </div>
            }
        </div>
        <div class="col-md-4" style="text-align:center;">
            @using (Html.BeginForm("EditBanner2", "Admin", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.Id)
            <p>Current image</p>
            <img src=@ViewBag.Picture2 style="width:90%; border-color:#1a1a1a; border-style:solid; border-width:.5em;" />
            <div class="editor-field" style="text-align:center; padding-top:1em;">
                <input id="Image" title="Upload new image"
                       type="file" name="file2" />
            </div>
            <div style="text-align:center; padding-top:1em;">
                    <input type="submit" value="Save Changes" class="btn btn-default" />
            </div>
            }
        </div>
        <div class="col-md-4" style="text-align:center;">
            @using (Html.BeginForm("EditBanner3", "Admin", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
            {
            @Html.ValidationSummary(true, "", new { @class = "text-danger" })
            @Html.HiddenFor(model => model.Id)
            <p>Current image</p>
            <img src=@ViewBag.Picture3 style="width:90%; border-color:#1a1a1a; border-style:solid; border-width:.5em;" />
            <div class="editor-field" style="text-align:center; padding-top:1em;">
                <input id="Image" title="Upload new image"
                       type="file" name="file3" />
            </div>
            <div style="text-align:center; padding-top:1em;">
                    <input type="submit" value="Save Changes" class="btn btn-default" />
            </div>
            }
        </div>
    </div>


<div>
    @Html.ActionLink("Back to List", "Index")
</div>
